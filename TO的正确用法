一、  
Local CQ : 通常是CQCQCQ六个字符，所有电台都会自动补齐至8位，表示只呼叫本地，即使中继台链接了网关/其他中继/反射器，也不会向外输出信号。  
你的信号只在中继台射频辐射范围内有效。

二、  
Gateway CQ: DR模式下，链接中继列表中的中继台，连接成功后，注意将 TO 改回 Use Reflector。否则每按一次PTT,你都会发射一次链接对方中继台的命令。  
链接成功后，你的信号在本地中继台范围和对方中继台范围内有效。

三、
Use Reflector: 在形式上与Local CQ一样都是CQCQCQ，不同的是，使用use reflector命令时，所有与中继台链接的网关/中继/反射器都能收到你的信号。

四、
/ 是一种特殊用法，先期没有DR模式的电台或者使用DV模式的时候，用来链接网关/中继/反射器。
比如连接到BR2SY的U段中继，可以使用/BR2SY B，注意，包括空格必须是8位字符。  
这个命令等效于BR2SY BL。
不同电台上，Reflector--Link to Reflector命令，使用以上两种链接格式之一。

五、  
U: 断开链接，正确的使用方法是七个空格+大写的U，等效于电台上Reflector--Unlink。
这个命令把所有连接到中继台上网关/中继/反射器断开（跟中继台物理链接的网关除外），慎用。

六、
I: 本地网关信息，正确的用法是七个空格+大写的I，等效于电台上Reflector--Infomation。  
这个命令让中继台播报现时网关状况，通常会收到本地网关已经链接的目的地。如果没有链接，则会收到语音Not Link(ed)。

七、
E: 回声测试，正确的用法是七个空格+大写的E，等效于电台上的Reflector--Echo。  
将 To 设置为回声测试后，按住PTT并对Mic讲话。松开PTT后，网关会将你的话音通过中继台播放出来。  
你可以通过回声测试对中继台和你自己电台的收发能力有一个感官了解。
